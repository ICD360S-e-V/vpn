# macOS / Xcode
.DS_Store
*.xcuserstate
*.xcuserdatad/
xcuserdata/
DerivedData/
build/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.swiftpm/
.build/
Package.resolved

# Go (vpn-agent)
agent/vpn-agent
agent/*.test
agent/coverage.out
agent/dist/
*.exe

# Secrets — NEVER commit
*.pem
*.key
*.crt
*.p12
*.pfx
*-private.key
*_private.key
secrets/
.env
.env.local

# IDE
.vscode/
.idea/
*.iml

# OS
Thumbs.db
.AppleDouble
.LSOverride

# Node (if we add tooling)
node_modules/

# Logs
*.log

# CLAUDE.md is the operational runbook with credentials, key paths,
# and other sensitive details. Lives at /root/CLAUDE.md on the
# production server (vpn.icd360s.de) and on local build hosts. The
# repo is public so this file must NEVER be committed under any name.
CLAUDE.md
**/CLAUDE.md
